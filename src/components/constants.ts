import { Upgrade, Chapter } from '../types';

// Placeholder for constants
export const UPGRADES: Upgrade[] = [];
export const CHAPTERS: Chapter[] = [{id: 0, name: 'Genesis', objective: 'Begin.', description: '', unlockCondition: () => false, quote: '', entityType: 'gas'}];
export const TUTORIAL_STEPS = [];
export const CROSSROADS_EVENTS = [];
export const NODE_IMAGE_MAP = {
    'player_consciousness': []
};
